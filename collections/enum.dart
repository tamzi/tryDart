
/*Declare an Enum outside a main function*/
enum students {Joanna, Phill, Patricia, Frank,Diana}
enum age {Twenty,Fifteen,Seven, Fourty, Thirty}

main(){
  print(students.values);
  print(students.Diana);
  print (students.Phill);
  print(age.Twenty);
}